/** @type {import('tailwindcss').Config} */
const config = {
  // Tailwind v4 uses CSS-first configuration via @import and @theme
  // Content scanning is handled via @source directives in CSS
}

export default config